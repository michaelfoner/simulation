For world simulation run, use the command:

```sh
php bin/console simulation:generate
```

For gun simulation run, use the command:

```sh
php bin/console simulation:generate gun
```

For unit test, use the command:

```sh
phpunit
```